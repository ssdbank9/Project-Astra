---
id: 01M33VD86SS216CCQEDVC9KPH6
title: Import tasks and Gantt from Excel/CSV
status: signoff
ready: true
creator: Claude
assignee: Claude
goal: "Astra can load action items and Gantt rows from a locked, Owner-configurable Excel template or a CSV: the App Owner or a project Manager downloads the template, uploads a filled file, reviews per-row validation, and commits in one transaction that creates or updates tasks by Import Key with full audit, a downloadable report, and role-correct authorization."
context: |-
  What is wrong today: users keep action items and Gantt charts in Excel (example: the Rupani Academy Professional Executive Gantt workbook, 19 rows, header on row 6, prose dates such as TBD and Immediate) and Astra has no way to load them; every task must be typed by hand.
  Trigger: Aly Jafferani asked in #astra-builder on 2026-09-22 for a platform where users load an Excel based on Astra-provided columns and the Gantt and action items are populated.
  Scope change from Aly on 2026-09-22 05:59 UTC: import is not Owner-only; Managers may import into projects they manage; only the Owner may create a project from a file; the template must be locked (sheet protection, dropdowns, date validation, version marker) and uploads must match its header row exactly; real dates only, no grid-derived dates.
  Known: design in scratchpad excel-import/design-spec.md and template-columns.json; a stdlib zipfile+xml.etree parser was proven on the workbook (workbook-dump.md); Astra has no third-party dependencies and pyproject.toml must stay unchanged.
  Ruled out: openpyxl or any new dependency; silently guessing prose dates; deleting tasks on re-import; overwriting an existing baseline.
definition-of-done: |-
  Template download: GET /api/import/template.xlsx (Tasks sheet locked except data rows, dropdowns, date validation, frozen header, version marker, README sheet) and /api/import/template.csv
  Parser: stdlib-only xlsx reader (shared strings, inline strings, missing dimension, absolute rel targets, 1900/1904 dates, serial <= 60 rejected) plus CSV path (utf-8 BOM, utf-8, cp1252)
  Preview: per-row OK/warning/error with messages and resolved values; header row must match the template exactly (CSV aliases allowed with a warning); no database write
  Commit: one BEGIN IMMEDIATE transaction; upsert by Import Key; never deletes; never overwrites a baseline; finish-to-start dependencies by key with cycle check; task_event per created/updated task; project import_committed event; imports row
  Report: GET /api/imports/<id>/report.csv downloadable
  Authorization: Owner imports anywhere and may create a project; Manager imports only into managed projects with protected actions skipped by warning; Viewer/Chairman/non-manager get 403 and an Owner notification; all tested
  UI: Import button for eligible users; three-step dialog Upload/Review/Confirm; no inline style attributes; keyboard accessible; Escape closes; focus returns
  Tests: full suite green with the new parser, importer and HTTP tests, node --check app.js, git diff --check
  Browser evidence: screenshots of Upload, Review, Confirm, dashboard and Gantt after import plus a phone Review shot, recorded with a Manager uploading
  Docs: README bullet, CONTEXT.md, docs/design/excel-import.md including how the Owner adds users before a Manager imports
tags:
  - astra
blocked-by: []
related: []
commits: []
created-at: 2026-09-22T06:03:49Z
updated-at: 2026-09-22T08:18:09Z
claimed-by: vm-2112
claimed-at: 2026-09-22T06:04:02Z
updated-by: Claude
outcome-what: "Independent review round 2 recorded: go. Round-1 HIGH/MEDIUM defects verified fixed against the code with probe-rerun.py (59/1/11) and 39 new adversarial probes (39 PASS, 0 FAIL, 14 NOTE incl. HTTP 409/413 and download roles); 170 tests OK, node --check and git diff --check clean, worktree clean."
outcome-why: "The diff satisfies every definition-of-done line with proofs that name real tests; the remaining seven findings are LOW (corrupt zip -> 500, closed-project download writes keys, XML control chars, two doc wordings, event label, CSV formula pattern) and belong in a follow-up ticket, so a person can now accept or send back."
outcome-resolves: "Review of fix commit c1f2f0b for the round-1 defects P17/P17b, P6a, P6c, P6d, P5b, P15c, P18, P1c, P12, P2d, P16b, P19, P7e, P21e and of the project-scoped pre-filled template download requested by Aly on 2026-09-22 07:18 UTC."
review-summary: "Round 2 of the independent review, on fix commit c1f2f0b (b449cbb..HEAD: importer.py +317/-, service.py +246, xlsx_reader.py +79, web.py, app.js, docs, tests +19 -> 170). What the diff ships: (1) the cross-project Import Key lookup (E_KEY_OTHER_PROJECT) is deleted, so T-001 may exist in every project as the per-project unique index always allowed. (2) Parent and dependency cycle checks now run on one node id per task (ImportEngine._node: task id when the key already exists in the project, file key otherwise) with parent_node resolving through existing tasks, so a cycle that passes through a task absent from the file, keyed or hand-made, is E_PARENT_CYCLE / E_DEP_CYCLE and nothing is written. (3) Invalid-row propagation moved into ImportEngine._propagate_invalid, run after dependency wiring and repeated until stable: a row whose parent or predecessor row errors (E_PARENT_INVALID / E_PRED_INVALID) is blocked too, so valid_rows_only can no longer import a step top-level or drop an edge silently; a three-row dependency cycle now errors all three rows. (4) parent_change is true when the new parent is a row created in the same file, applied in pass 2 after creation, with a parent_key entry in changes. (5) header.manager_email is escaped in renderImportReview; a test pins that no bare member interpolation reaches innerHTML in the dialog. (6) xlsx_reader returns Percent(displayed value) for %-formatted cells (built-in 9/10 and custom codes), parse_progress/parse_number use it, so a 100% cell stores 100; caps are 8 MB per zip part and 20 MB total on declared sizes (XlsxTooLarge -> ImportTooLarge -> HTTP 413), bare serial dates follow date1904, unheaded extra data is W_EXTRA_DATA, a same-key race is caught as sqlite3.IntegrityError -> ImportConflict -> 409 with rollback, Owner new-project imports grant row users membership (manager for the Project-sheet manager, else member) instead of dropping them, changes_requested joins NO_IMPORT_ON_UPDATE (W_GOVERNED_STATUS for the Owner too), the JSON 1 MB ceiling is applied by route (RAW_BODY_ROUTES) not Content-Type, and W_TITLE_CHANGED plus a README line warn about shifted pre-filled keys. New feature: GET /api/import/template.xlsx|csv?project_id= (AstraService.import_template -> _template_project for Owner-any / Manager-of-project / else 403, 404 unknown; _template_prefill assigns T-nnn keys to unkeyed tasks inside one transaction with import_key_assigned events and notify=False, fills every enabled column so an unedited re-upload is N unchanged, orders parents before steps, offsets the blank-row key formula so the first blank row yields the next free number); the dialog links switch to \"Download template (with this project tasks)\" on target change."
review-gaps: "Round-1 defects: all six re-verified fixed by probe-rerun.py (PASS 59 / FAIL 1 / NOTE 11; the FAIL is P18 feeding an UNformatted 1.0, which Excel shows as 1 - a real 100%-formatted cell reads 100 in test_percent_formatted_cells_are_read_as_displayed and my R5a) and by 39 new probes in review/probe-round2.py (39 PASS, 0 FAIL, 14 NOTE; report review-probes-round2.md): 3-node dependency and parent cycles through existing tasks, through a hand-made task with no key, and mixed new+existing rows are all refused with the DB untouched; re-parent under an erroring new row blocks the child; children and successors of a row that errors are blocked under valid_rows_only; 100%/50% cells -> 100/50, bare 0.75 -> 75, 8 MiB+1 and 21 MiB forged sizes -> 413 (exactly 8 MiB parses); download roles Owner/Manager-managed 200, Manager-unmanaged/Viewer/Chairman/outsider 403, unknown 404 (service and HTTP); keys assigned exactly once across four downloads, existing RA keys kept, zero Owner notifications; unedited re-upload = 0 create / N unchanged as Owner, as Manager, via CSV, in Simple and Full; the pre-filled file uploaded to another project is E_PROJECT_MISMATCH on every row; a row inserted in the middle of literal keys is E_KEY_MISSING, in a formula-key file it is W_TITLE_CHANGED on every shifted row; the first blank-row formula (also with a negative offset, e.g. ROW()-1-2) yields the next free key and a row added at the bottom creates 1 / leaves N unchanged; concurrent duplicate key -> HTTP 409 with 0 tasks and 0 imports; Owner re-import of Changes requested/Completed/Submitted/On hold/Reopened -> unchanged + W_GOVERNED_STATUS; manager_email markup verbatim in JSON and escaped in the DOM builder; 2 MB non-JSON body on /api/projects -> 400; billion-laughs entities refused by expat 2.6.1. Remaining, worst first, none blocking: (1) LOW-MEDIUM xlsx_reader.py:226-230 _read.parse catches only ET.ParseError; a part whose central-directory size is smaller than its content (zipfile.BadZipFile: Bad CRC-32) or whose deflate stream is corrupt (zlib.error) escapes as HTTP 500 Internal server error with an Unhandled error log line (R6d, R6e, R14e, R14f). Fix: wrap archive.read(name) in try/except (zipfile.BadZipFile, zlib.error, EOFError, OSError) -> XlsxError(\"Workbook part <name> is corrupt or truncated; save the file again from Excel\"). Authorized uploaders only, nothing written. (2) LOW service.py:2332 _template_project does not refuse a closed project the way _import_authorize does, so GET /api/import/template.xlsx?project_id=<closed> by its Manager assigns import keys and writes import_key_assigned events into a closed project (R7b). Fix: raise ValueError(\"This project is closed\") or skip the assignment. (3) LOW importer.py:361-368 _xml/_xml_text escape markup but not characters illegal in XML 1.0 (\\x0b, \\x0c and other C0 controls); one task title or description holding such a character makes the whole pre-filled download a file that Excel and read_workbook refuse (\"not well-formed XML\", R13a). Fix: strip [^\\t\\n\\r\\x20-퟿-�] before escaping. (4) LOW docs/design/excel-import.md:201 says \"45% and 0.45 accepted\" but a TEXT 0.45 (any CSV cell, or a text cell in xlsx) is E_PROGRESS_INVALID \"not a whole number\"; only a numeric 0.45 cell is scaled (R5b, R5c); and :116 \"no warnings\" on an unedited re-upload holds only while every task owner still has project access - an owner without access gives W_PERSON_NOT_ELIGIBLE with the owner kept (R8b-e). (5) LOW app.js:301 EVENT_LABELS has no entry for import_key_assigned, so a task history shows the raw kind (R15). (6) LOW, pre-existing pattern: build_template_csv writes a title beginning with = bare, like the export CSV (CSV formula injection when opened in Excel); one follow-up for both. (7) Cosmetic: a parent cycle closed by a new row plus a re-parented existing row is reported on the new row (R2b); both rows are blocked. Round-1 lows still open as designed: P14f custom label may equal a disabled built-in label; blank template downloadable by any signed-in user (documented). Not supported by the diff: nothing material - the outcome account matches the code, the 170 tests, and my probe numbers exactly. DoD: every line has a proof that names an existing test or path; the import dialog section of app.js (lines 856-1104) has zero style attributes (the 18 elsewhere are the disclosed pre-existing Gantt bars); shots 01-12 exist under scratchpad/excel-import/shots."
review-verdict: "go: the two HIGH (cross-project key check, cycles through tasks absent from the file) and three MEDIUM (re-parent under a new row, late parent/predecessor propagation, unescaped manager email) defects from round 1 are fixed in the code, not only in the account, and hold against 39 new adversarial probes plus the 170-test suite; the project-scoped pre-filled download does what Aly asked (keys assigned once and persisted, unedited round trip unchanged, roles enforced over HTTP). Seven LOW items remain (corrupt zip -> 500 instead of 400, closed-project download writes keys, control characters break the pre-filled workbook, two doc wordings, a missing event label, CSV formula-injection pattern); none violates the definition of done and each has a one-line fix; ship them as a follow-up ticket."
review-check: "1. cd /workspace/astra-import && .venv/bin/python tests/run.py  -> the last lines read \"Ran 170 tests\" and \"OK\".  2. node --check src/astra/static/app.js && git diff --check && git status --short  -> no output from any of the three.  3. .venv/bin/python /tmp/claude-0/-workspace/05dedfc2-2370-5849-91ce-57fb10b158df/scratchpad/excel-import/review/probe-round2.py  -> the last line reads SUMMARY: PASS 39, FAIL 0, NOTE 14; every [NOTE] line is one of the LOW items listed in review-gaps.  4. .venv/bin/python /tmp/claude-0/-workspace/05dedfc2-2370-5849-91ce-57fb10b158df/scratchpad/excel-import/review/probe-rerun.py  -> SUMMARY reads PASS 59, FAIL 1, NOTE 11 and the only [FAIL] is P18 (its fixture writes an unformatted 1.0; a real 100% cell is covered by step 3, probe R5a).  5. Hand check of the fix that mattered most: .venv/bin/astra serve --host 127.0.0.1 --port 8765, sign in as the App Owner, create projects A and B, press Import, leave the target on B, download the .xlsx, type a title in Tasks row 2 (cell A2 fills itself with T-001), save, upload with target A, Preview, Import 1 row; then download again, type a different title in row 2 (A2 is T-001 again), upload with target B, Preview  -> the row is green/OK and \"Import 1 row\" is enabled (round 1 showed a red \"already belongs to project A\" error here).  6. Pre-filled download: in project A add a task by hand named \"Typed by hand\" (no import), press Import, choose A as target  -> the link text changes to \"Download template (with this project tasks) (.xlsx)\"; download it and open it in Excel  -> the Tasks sheet lists T-001 and the hand-made task with a literal key T-002 in column A, row 4 column A is empty until you type a title, then shows T-003.  7. Upload that file unchanged to project A and Preview  -> the summary chips read Create 0, Update 0, Unchanged 2, Errors 0; open the task \"Typed by hand\" in Astra  -> its history has an entry whose label is the raw word import_key_assigned (the LOW label gap).  8. In the same file change the title in row 2, upload, Preview  -> exactly one row is marked Update with a yellow warning \"Title changes from ... to ...; if you inserted or deleted rows, the pre-filled keys below that point have shifted\"; Import it.  9. Cycle check by hand: Template settings -> Full -> Save, download a fresh template, rows A (title a) and B (title b, Predecessors A), Import 2 rows; then upload a file with only row A whose Predecessors cell is B, Preview  -> row A is red with \"Depending on B would create a cycle\" and the Import button is disabled (round 1 imported it and blocked both tasks).  10. The remaining LOW defect, to see it once: open the downloaded .xlsx in a hex editor or run: .venv/bin/python -c \"import sys;sys.path[:0]=[\\\"src\\\",\\\"tests\\\"];from import_fixtures import forge_declared_size;d=open(sys.argv[1],\\\"rb\\\").read();open(\\\"/tmp/bad.xlsx\\\",\\\"wb\\\").write(forge_declared_size(d,\\\"xl/worksheets/sheet3.xml\\\",10))\" <path to the downloaded xlsx>, upload /tmp/bad.xlsx, Preview  -> the dialog shows \"Internal server error.\" instead of a message about a corrupt file; the server log prints \"Unhandled error: BadZipFile\". Nothing is written either way (tasks unchanged)."
---

# Import tasks and Gantt from Excel/CSV

## Definition of Done

- [x] Template download: GET /api/import/template.xlsx (Tasks sheet locked except data rows, dropdowns, date validation, frozen header, version marker, README sheet) and /api/import/template.csv
  proof: tests/test_import.py::test_project_template_is_prefilled_assigns_keys_and_round_trips_unchanged, ::test_project_template_authorization_and_simple_preset, ::test_project_template_download_over_http_follows_roles; service.import_template(actor, fmt, project_id) + web GET ?project_id=
- [x] Parser: stdlib-only xlsx reader (shared strings, inline strings, missing dimension, absolute rel targets, 1900/1904 dates, serial <= 60 rejected) plus CSV path (utf-8 BOM, utf-8, cp1252)
  proof: src/astra/xlsx_reader.py; tests/test_xlsx_reader.py (8 tests); CSV path tests/test_import.py test_csv_path_accepts_aliases_with_a_warning_and_bom
- [x] Preview: per-row OK/warning/error with messages and resolved values; header row must match the template exactly (CSV aliases allowed with a warning); no database write
  proof: tests/test_import.py::test_child_of_a_parent_that_fails_in_dependency_wiring_is_an_error_too (E_PARENT_INVALID/E_PRED_INVALID propagated after dependency wiring, importer.ImportEngine._propagate_invalid), ::test_data_in_an_unheaded_extra_column_is_reported (W_EXTRA_DATA), ::test_title_change_on_reimport_warns_and_readme_says_to_add_rows_at_the_bottom (W_TITLE_CHANGED), ::test_preview_reports_rows_and_writes_nothing
- [x] Commit: one BEGIN IMMEDIATE transaction; upsert by Import Key; never deletes; never overwrites a baseline; finish-to-start dependencies by key with cycle check; task_event per created/updated task; project import_committed event; imports row
  proof: tests/test_import.py::test_dependency_cycle_through_an_existing_task_absent_from_the_file_is_refused (P6a), ::test_parent_cycle_through_an_existing_task_absent_from_the_file_is_refused (P6c), ::test_existing_task_reparented_under_a_row_that_is_new_in_the_same_file (P6d), ::test_same_import_key_is_allowed_in_two_projects (P17), ::test_duplicate_key_race_during_commit_is_a_conflict_and_rolls_back (409), ::test_owner_reimport_does_not_set_changes_requested_without_a_submission; cycle graphs over importer.ImportEngine._node
- [x] Report: GET /api/imports/<id>/report.csv downloadable
  proof: web.py GET /api/imports/<id>/report.csv -> AstraService.import_report; test_preview_commit_report_and_conflict_over_http
- [x] Authorization: Owner imports anywhere and may create a project; Manager imports only into managed projects with protected actions skipped by warning; Viewer/Chairman/non-manager get 403 and an Owner notification; all tested
  proof: tests/test_import.py::test_manager_into_unmanaged_project_viewer_and_chairman_are_blocked_and_owner_notified, ::test_manager_imports_into_own_project_with_protected_actions_downgraded, ::test_owner_creates_project_from_the_project_sheet (row owners granted member/manager on the new project), ::test_project_template_authorization_and_simple_preset (Manager 403 on unmanaged project template), ::test_project_template_download_over_http_follows_roles
- [x] UI: Import button for eligible users; three-step dialog Upload/Review/Confirm; no inline style attributes; keyboard accessible; Escape closes; focus returns
  proof: src/astra/static/app.js renderImportUpload (project-scoped download links, updateTemplateLinks) and renderImportReview (escapeHtml(header.manager_email)); tests/test_import.py::test_preview_returns_project_sheet_values_verbatim_and_the_dialog_escapes_them, ::test_project_template_download_over_http_follows_roles (dialog copy); node --check clean
- [x] Tests: full suite green with the new parser, importer and HTTP tests, node --check app.js, git diff --check
  proof: .venv/bin/python tests/run.py -> Ran 170 tests, OK (151 before the review fixes; +19: 17 in tests/test_import.py, 2 in tests/test_xlsx_reader.py incl. forged zip sizes and Percent cells); node --check src/astra/static/app.js; git diff --check clean
- [x] Browser evidence: screenshots of Upload, Review, Confirm, dashboard and Gantt after import plus a phone Review shot, recorded with a Manager uploading
  proof: scratchpad/excel-import/shots/01..12 (Upload, raw workbook rejected, prose-date errors, Review, Confirm, dashboard, Gantt, detail, re-import, phone Review, Template settings incl. presets, Owner inbox) recorded with Waseem (Manager) uploading astra-import-sample-rupani-simple.xlsx; browser-evidence.md
- [x] Docs: README bullet, CONTEXT.md, docs/design/excel-import.md including how the Owner adds users before a Manager imports
  proof: docs/design/excel-import.md: 'Project-scoped download' section, upload limits (8 MB/part, 20 MB total, 413), W_EXTRA_DATA, date1904 bare serials, Percent cells, propagation and unified cycle graphs, W_TITLE_CHANGED, 409 on key race, changes_requested never set on update, new-project access grants, dialog escaping
- [x] Template configuration: Owner-only PUT /api/import/template-config (Managers may GET); enable/rename/reorder built-ins, core columns locked, custom text/number/date/list columns; template and upload validation follow the configuration; stale template rejected; custom values in tasks.import_extras shown read-only in task detail; tested
  proof: importer.TemplateConfig, AstraService.get_/set_import_template_config, web.py do_PUT; tests test_template_config_round_trip_custom_column_and_stale_template, test_template_config_validation_and_authorization, test_template_config_over_http_and_stale_template_rejected
- [x] Dates inside the import feature display as dd-mm-yyyy (template format, README, Review table, report) and the parser accepts dd-mm-yyyy text as well as ISO and Excel dates
  proof: importer.DISPLAY_DATE_FORMAT/display_date/parse_date_cell (DMY_DATE); tests assert '01-09-2026' in preview values and report; template styles formatCode dd-mm-yyyy
- [x] Workbook v2 parity: generated template has the 7 sheets (Lists hidden, _astra veryHidden), Tasks header equals config labels, _astra!B1 equals the config hash; Type=Milestone maps to is_milestone; Project sheet is the project header; People sheet cross-checked in preview; EXAMPLE-/EX- keys refused
  proof: tests test_simple_preset_is_the_default_and_full_is_one_call_away, test_template_workbook_is_locked_validated_and_marked (7 sheets under Full, header == labels, _astra!B1 == hash), test_commit_creates_tasks_events_dependencies_and_import_record (Type=Milestone -> is_milestone), test_owner_creates_project_from_the_project_sheet (Project + People sheets), test_template_download_round_trips_through_the_parser_and_example_keys_are_refused
Parser: stdlib-only xlsx reader (shared strings, inline strings, missing dimension, absolute rel targets, 1900/1904 dates, serial <= 60 rejected) plus CSV path (utf-8 BOM, utf-8, cp1252)
Preview: per-row OK/warning/error with messages and resolved values; header row must match the template exactly (CSV aliases allowed with a warning); no database write
Commit: one BEGIN IMMEDIATE transaction; upsert by Import Key; never deletes; never overwrites a baseline; finish-to-start dependencies by key with cycle check; task_event per created/updated task; project import_committed event; imports row
Report: GET /api/imports/<id>/report.csv downloadable
Authorization: Owner imports anywhere and may create a project; Manager imports only into managed projects with protected actions skipped by warning; Viewer/Chairman/non-manager get 403 and an Owner notification; all tested
UI: Import button for eligible users; three-step dialog Upload/Review/Confirm; no inline style attributes; keyboard accessible; Escape closes; focus returns
Tests: full suite green with the new parser, importer and HTTP tests, node --check app.js, git diff --check
Browser evidence: screenshots of Upload, Review, Confirm, dashboard and Gantt after import plus a phone Review shot, recorded with a Manager uploading
Docs: README bullet, CONTEXT.md, docs/design/excel-import.md including how the Owner adds users before a Manager imports

## Options

- [ ] brainstorm
- [x] planning

## Plan

<Steps, in order — filled in by the pre-process step, or by you.>

- [x] Schema v13 in src/astra/db.py: tasks.import_key (unique per project), tasks.is_milestone, tasks.next_action_note, imports table
- [x] src/astra/xlsx_reader.py: stdlib zip+xml reader (sheets, rels, shared/inline strings, styles for date detection, 1900/1904, missing dimension, defined names, hidden sheets)
- [x] src/astra/importer.py: template column contract, locked template builder (xlsx via zipfile, csv), upload parser with exact-header check and CSV aliases, vocab tables, finding codes, row validator and commit plan
- [x] src/astra/service.py: import_targets, import_preview, import_commit, list_imports, import_report; authorization Owner/Manager with blocked-attempt audit + notification; next_action_note in _next_action
- [x] src/astra/web.py: raw-body branch (5 MB), template routes, targets, preview, commit (409 on sha mismatch, 413 oversize), imports report route
- [x] UI: Import button + three-step dialog in index.html/app.js/style.css, class-based styling, keyboard/focus handling
- [x] Tests: tests/test_xlsx_reader.py, tests/test_import.py (service + HTTP), keep delegation test green
- [x] Docs: README, CONTEXT.md, docs/design/excel-import.md
- [x] Browser evidence with fixture server (Waseem as Manager) and Playwright; kill server; verify clean git status
- [x] Push branch, open draft PR, move ticket to review
- [x] Rewrite template builder to the v2 seven-sheet structure with named-range dropdowns, CF rules, People formulas; make v2 config the default; parse Project/People sheets

## Progress
- **2026-09-22 06:12 · Claude** — Scope change 2 (Aly, 06:07 UTC): dates display dd-mm-yyyy within the import feature only (template number format, README, Review table, report; parser also accepts dd-mm-yyyy text). Owner-configurable template stored in import_template_config (enable/rename/reorder built-ins, add custom text/number/date/list columns, core columns locked); custom values land in tasks.import_extras JSON and show read-only in task detail; template hash marker replaces the fixed version number so stale templates are rejected. Endpoints GET/PUT /api/import/template-config. Schema v13 now also carries import_template_config and tasks.import_extras.
- **2026-09-22 06:32 · Claude** — Decisions while implementing: (1) dependency problems (unknown key, self, cycle) are row errors that block the row, not warnings that import the rest of the row - one rule 'errors block their row' is easier to explain than a per-column exception. (2) Managers' protected status changes are skipped with W_PROTECTED_STATUS rather than filed as owner_action_requests: the request path needs a task and a single action; a bulk import would create one request per row with no execution path yet (Gate 3). (3) The template example row uses key EXAMPLE-001 and the importer refuses it (E_EXAMPLE_ROW) so a half-filled template cannot import sample data. (4) Sheet protection uses the documented SHA-512 spin hash with a fixed documented password; it guards against accidents, not against a determined editor, and the docs say so. (5) Version marker = 16 hex chars of sha256 over the active column shape, written to hidden sheet _astra B1 and a defined name; exact header comparison is done in addition, so a renamed header is reported by column letter. (6) export CSV columns were left unchanged to avoid touching the export tests; import_key is already in the JSON export via t.*.
- **2026-09-22 06:36 · Claude** — Scope change 3 (Aly, via coordinator): adopt the researched 7-sheet workbook v2 (README, Project, Tasks, Example, People, Lists hidden, _astra veryHidden) as the default template; template-config-v2.json becomes the built-in default (18 of 25 columns on; custom x_type list and x_risk_dependency text). Importer maps Type=Milestone to is_milestone, reads the Project sheet as the project header (Owner may create from it; Manager's must match the chosen project), cross-checks the People sheet in preview, ignores Example and refuses keys starting EXAMPLE-/EX-. Stdlib limits vs build.py: header hover comments (VML part) are not emitted; guidance lives in input messages and README.
- **2026-09-22 07:07 · Claude** — Scope change 4 (Aly, 06:52 UTC): the default template is the Simple preset (nine columns: Import Key, Title, Step of (Key), Owner Email, Start Date, Due Date, Status, Criticality, Notes; hash 52cd3da7813769c1, equal to the design worker's template-config-simple.json); Full (v2, 18 columns, hash 53133bf237fe9b3c) is a one-click preset in Template settings. Title keeps its label (core columns are not renamed). Import Key cells carry =IF(B2="","","T-"&TEXT(ROW()-1,"000")) so untouched rows stay blank; the parser also skips rows whose only cell is a key. Simple workbook: 8-line README, 3-row Project sheet, 2-row Example, no People sheet; any column beyond the simple set switches the generator to the extended shape. Browser evidence redone with the simple sample. Pre-existing defects seen but not touched: Gantt bars use inline style attributes blocked by CSP; dashboard overflows horizontally at 1440px (Gate 2).
- **2026-09-22 07:24 · Claude** — Independent review (no-go, ticket stays in review). HIGH importer.py:1896 E_KEY_OTHER_PROJECT rejects any key used in another project although the index is per project; with the Simple template's prefilled T-001 keys the second project cannot import (probe P17). HIGH importer.py:2147-2197 and 2096-2110: cycle graphs mix keys and task ids (_node_for_task maps an existing task to its key only when the key is in the file), so cycles through existing tasks absent from the file are missed and the commit writes dependency (P6a) and parent (P6c) cycles. MEDIUM importer.py:2330 parent_change false for an existing task re-parented under a new row (P6d). MEDIUM importer.py:2114 parent/predecessor that errors in dependency wiring is not propagated; child imports top-level silently with valid_rows_only (P5b). MEDIUM app.js:954 header.manager_email unescaped in innerHTML (P15c). LOW-MEDIUM 100% percent cell stored as 1% (xlsx_reader.py:348, importer.py:1635, P18). LOW: =ROW() prefilled keys renumber on row delete (P11b), per-part 50 MB cap allows 800 MB RSS from a 3.4 MB upload (P1c), bare serial ignores date1904 (P12), unheaded extra data dropped silently (P2d), same-key race -> 500 (P16b), new-project import drops all owners incl. the appointed manager (P19), Owner may set changes_requested on re-import (P7e), non-JSON Content-Type bypasses the 1 MB JSON limit (P21e). Probe scripts and results: scratchpad/excel-import/review/{probe.py,probe2.py,review-probes.md,template-simple.xlsx,template-full.xlsx}. Everything else in the DoD verified: 151 tests, node --check, diff --check, template structure via openpyxl, authorization matrix for all four roles, atomicity, sha 409, 413, design sample 19 creates then 19 unchanged.
- **2026-09-22 07:54 · Claude** — Review fixes, decisions: (1) E_KEY_OTHER_PROJECT removed outright rather than downgraded to info - the index is per project and an info finding on every T-001 row of every second project is noise. (2) Cycle graphs now use one node id per task (task id when the task exists in the project, file key otherwise; ImportEngine._node); no separate 'key in file' mapping. (3) Error propagation (_propagate_invalid) runs after dependency wiring and is strict: a row whose parent OR predecessor row errors is an error, matching the docs' 'errors block their row and any row whose parent or predecessor they are'. Consequence: a 3-row dependency cycle now errors all three rows (test_dependency_cycle_* updated from create=2 to nothing importable) instead of importing two rows with a silently dropped edge. (4) Percent cells: xlsx_reader returns Percent(float) = displayed percentage, so parse_progress can tell a %-formatted 0.5 (50%) from a bare 0.5 (heuristic 50) and a %-formatted 0.005 (0.5%, refused as not whole). (5) New-project Owner import: active users named in rows are eligible and granted at commit (header manager -> manager, others member, INSERT OR IGNORE so the manager is not downgraded); People-sheet status for them is now ok, test updated. (6) changes_requested joins the statuses an import never sets on update (NO_IMPORT_ON_UPDATE), warning W_GOVERNED_STATUS for the Owner too. (7) Zip caps 8 MB/part, 20 MB total -> XlsxTooLarge -> ImportTooLarge -> HTTP 413; the >5 MB service check also raises ImportTooLarge now. (8) JSON routes enforce the 1 MB ceiling by path (RAW_BODY_ROUTES = preview, commit), not by Content-Type.
- **2026-09-22 07:57 · Claude** — Project-scoped template download (Aly 07:18 UTC), decisions: (1) GET /api/import/template.xlsx|csv?project_id= -> AstraService.import_template(actor, fmt, project_id) -> _template_project (Owner any, Manager of that project, else Forbidden/403; unknown 404) and _template_prefill. (2) Keys for tasks without import_key are assigned INSIDE the download (one transaction, importer.assign_sequence_keys continuing above the highest T-nnn, task_event import_key_assigned written with _event(notify=False) so a Manager's download does not flood the Owner's inbox). Alternative rejected: assigning keys at commit time - then the downloaded file could not carry literal keys and a re-upload would create duplicates. (3) Row values were chosen for a clean round trip (re-upload unedited = N unchanged, 0 warnings): Description = full stored description, Notes = the description's 'Notes:' section (importer.notes_section), Original Due Date blank, Type only from import_extras, Milestone Yes/No. (4) Blank rows keep the key formula with an offset (importer.next_key_offset -> _TemplateWorkbook(key_offset)); the exact old formula string is unchanged when the offset is 0 so the existing test stays valid. (5) Ordering: parents before steps, each level by Start, Due, Title (not created_at). (6) CSV twin gets the same rows as dd-mm-yyyy text. UI: links get ids import-template-xlsx/csv and change href+label on target select change. Probe delta: reviewer's probe.py stops at P5b now because that whole file is refused (expected); probe-rerun.py (P5b commit wrapped in try) gives PASS 59 / FAIL 1 / NOTE 11 vs 51/9/13 - the one FAIL (P18) writes an UNformatted 1.0 (fixture has no style), which Excel shows as 1, so 1% is right; a real 0%-formatted cell is covered by test_percent_formatted_cells_are_read_as_displayed.
- **2026-09-22 08:18 · Claude** — Independent review round 2 (go, moved to signoff). Round-1 HIGH/MEDIUM defects verified fixed in code by probe-rerun.py (PASS 59 / FAIL 1 / NOTE 11, the FAIL is the unformatted-1.0 fixture) and 39 new probes in scratchpad/excel-import/review/probe-round2.py (PASS 39 / FAIL 0 / NOTE 14, report review-probes-round2.md): 3-node and hand-made-task cycles, mixed new/existing cycles, erroring new parent, valid_rows_only propagation, percent cells, forged zip sizes at the 8 MiB and 20 MiB boundaries, download roles at service and HTTP level incl. closed project, keys assigned once, unedited round trip in Simple/Full/CSV as Owner and Manager, file of project A into project B, row inserted in the middle (literal and formula keys), rows added at the bottom, concurrent duplicate key -> 409 over HTTP, governed statuses, manager_email escaping, JSON ceiling, billion-laughs entities. Remaining, all LOW, for a follow-up ticket: (1) LOW-MEDIUM xlsx_reader.py:226-230 corrupt/truncated zip part (zipfile.BadZipFile Bad CRC-32, zlib.error) escapes as HTTP 500 - catch around archive.read and raise XlsxError. (2) LOW service.py:2332 _template_project lets a Manager download a CLOSED project template, assigning import keys into it - add the closed check from _import_authorize. (3) LOW importer.py:361-368 _xml/_xml_text do not strip XML-illegal control characters (\x0b, \x0c); one such task makes the pre-filled workbook unreadable by Excel and read_workbook. (4) LOW docs excel-import.md:201 text 0.45 is refused (only numeric 0.45 cells scale) and :116 unedited re-upload warns W_PERSON_NOT_ELIGIBLE when a task owner has no project access. (5) LOW app.js:301 EVENT_LABELS lacks import_key_assigned. (6) LOW pre-existing: CSV template and export write =-prefixed cells bare (formula injection). (7) cosmetic: mixed new/existing parent cycle reported on the new row. Open from round 1 by design: P14f, blank template for any signed-in user.
