---
id: 01M32AMMEW7SGBE4NRKTD73AQW
title: "Gantt: visible colour-coded task steps with hover and click detail"
status: signoff
ready: true
creator: Claude
assignee: Claude
goal: "Make a task's steps (its subtasks) visible inside the parent task bar on the Portfolio Gantt, each step with its own colour and index, with hover/keyboard tooltip and click-through to the step's own record, plus a schedule-table twin, without adding libraries or changing authorization."
context: |-
  What is wrong today: a task's steps are invisible inside its Gantt bar. Subtasks (tasks with parent_task_id) render as separate full rows in src/astra/static/app.js renderGantt (lines 83-96), and the parent bar is one plain span.
  The bar is not interactive: no tooltip, no hover, no focus; the only click target is the 'Details & history' link in the name column (app.js:305 delegated [data-detail] handler).
  Trigger: Aly (App Owner) asked on 2026-09-21 in Slack for steps to be visible in the Gantt, a separate colour for each small step, and hover or click to see the step's details and owner.
  Steps = subtasks. The data already exists: tasks.parent_task_id, owner_user_id, status, criticality, start_date, due_date, progress (src/astra/db.py:103-121). GET /api/tasks returns all tasks flat with parent_task_id, so the client can group with no new endpoint.
  Gap: service.list_subtasks (src/astra/service.py ~1248) returns only id, title, status, due_date, owner_name; no start_date, so the detail dialog cannot show step dates.
  Research: scratchpad gantt-steps-research.md compared Asana, monday, Smartsheet, MS Project, TeamGantt, ClickUp, Wrike, Notion, DHTMLX, Bryntum, Frappe. Only DHTMLX split-mode draws steps inline in the parent bar when collapsed and as rows when expanded; that hybrid is the model.
  Approved defaults (Aly, 2026-09-21): (1) steps collapsed inline in the parent bar by default, chevron expands to child rows; (2) colour = step identity in index order using the Okabe-Ito colour-blind-safe palette, owner shown as initials chip and in the tooltip; (3) click opens the step's own task record with a Parent back-link.
  Constraints from docs/design/astra-product-ux-baseline.md: native HTML/CSS/JS only, colour never the sole carrier of meaning, WCAG 2.2 AA, hover AND keyboard focus, reduced motion respected, structured schedule-table alternative, 44px targets, no precision Gantt on phones.
  Known: the CSP header (src/astra/web.py:465) is style-src 'self', so inline style attributes are blocked; positions must be set through the CSSOM from JS.
  Ruled out: a new endpoint for rendering; colour = owner (conflicts with colour = step); the title attribute for tooltips.
definition-of-done: "Dated subtasks render as numbered, coloured segments inside the parent task bar on the Gantt (one Okabe-Ito hue per step index, index label always visible, title when the segment is wide enough); hover and keyboard focus show a tooltip with step name, owner, start/due dates, duration, status and due state, dismissible with Escape; clicking or pressing Enter/Space on a step opens that step's detail dialog, which shows a Parent back-link to the parent task; a chevron expands/collapses the child rows under the parent and the choice is remembered per parent in the browser; undated steps are shown as an 'n steps need dates' chip and never given invented dates; colour is never the sole carrier: every segment carries its index and a legend lists index + colour; a Schedule table toggle shows the same data as a real table (Project, Task, Step #, Step, Owner, Start, Due, Status, Due state, Critical path) and is the default at phone width; prefers-reduced-motion disables the transitions; steps narrower than 8px fold into a +N overflow disclosure; service.list_subtasks returns start_date, criticality, progress, parent_task_id and owner_user_id with unit and HTTP tests for the shape; node --check on app.js and the full test suite pass; browser evidence captured for Owner and Viewer at 1440 and 390 widths."
tags:
  - astra
blocked-by: []
related: []
commits:
  - 33d237f8fc11c712722c48e9d8834f63ed4dc0eb
  - b5ddafa
  - ad0549b6db535518705b2dd1817b701a21a5a546
created-at: 2026-09-21T15:51:31Z
updated-at: 2026-09-21T16:40:39Z
updated-by: Claude
claimed-by: vm-13959
claimed-at: 2026-09-21T16:27:02Z
outcome-what: "Fixed the two review defects on the Gantt steps: Escape now dismisses a hover-only tooltip (document-level keydown) and lane-2 steps on two-lane tracks keep their own hit area (per-lane pseudo hit areas plus z-index on .lane-2); +N chip relabelled to cover lane-overflow steps."
outcome-why: "Independent review (3fccce6) returned no-go: the DoD Escape clause failed for hover-only tooltips and overlapping steps on two-lane bars routed hover/click to the wrong step."
outcome-resolves: "Both review gaps 1 and 2 verified in headless Chromium (scratchpad/gantt/fix-probe.json): hover-only Escape hides #step-tip with focus on body; elementFromPoint at 1/4/7px inside a lane-2 step under a lane-1 step returns the lane-2 step and hover names it. node --check clean, 119 tests OK, git diff --check clean. Minor notes 3 (label) fixed; 4, 5, 7 left as follow-ups."
review-summary: "Re-review of 3fccce6..7a0336a (fix ad0549b) on top of the full feature. Since the last review two source files changed. app.js (wireGantt): a second keydown listener on document now calls hideTip() and closeMoreLists() on Escape, so the key works while focus is still on body (a hover-only tooltip or an open +N list); the #gantt keydown Escape branch for a focused step is kept. Both functions are no-ops when nothing is open (hideTip clears the timer, drops aria-describedby only if a target is recorded, sets #step-tip hidden; closeMoreLists only touches lists that are not hidden) and neither calls preventDefault, so native dialog Escape and typing in inputs are untouched. style.css: on .bar.track.lanes-2 the 44px pseudo hit area is split per lane (lane 1 ::before -13px/-3px, lane 2 ::before -3px/-13px) and .lane-2 gets z-index 1, so a later lane-1 step no longer paints over the visible top of an overlapping lane-2 step; single-lane tracks keep the full -13px/-13px (44px) area. The +N chip aria-label and list heading now read \"N more steps not drawn at this scale (too small or overlapping)\" because that list also holds steps pushed out by the lane limit. Overall the ticket delivers: parent tracks with numbered Okabe-Ito segments per dated subtask, shared role=tooltip on hover and focus with owner, UTC dates, duration, status and due state, click/Enter/Space to the step dialog with a Parent back-link, chevron expand remembered in localStorage, undated chip, legend, Schedule table twin (default under 760px), reduced-motion block, +N overflow, and list_subtasks extended read-only with unit and HTTP tests. web.py untouched, no new route or write path. Verified hands-on in headless Chromium at 1440x900 and 390x844 against a server-side fixture (real Overlap parent with steps A Sep 1-Nov 30, B Oct 15-Mar 31, C Dec 1-Feb 28): hover step 1 with focus on body, Escape -> #step-tip hidden, aria-describedby removed; focused-step Escape still hides; Enter opens the step dialog with \"Parent: Quarterly board pack\" and Escape closes the native dialog with focus returning to the step; Escape inside the New task form with a focused input still closes that dialog and no page error; elementFromPoint at 1, 4, 7, 10 and 17px inside B under A and under C returns B, A and C hit themselves at 1, 9, 17px; measured hit heights lane 1 34px, lane 2 34px, single lane 44px; hovering the top of B under C shows \"Step 2 of 3 - B\" and clicking there opens B with \"Parent: Overlap\"; +N list opened with focus on body closes on Escape; table shows the ten columns with Overlap and its three step rows; reduced motion -> transition 0s; 390px with no stored preference defaults to the table with no sideways scroll. Suite 119 OK, node --check clean, git diff --check clean. Only console errors are the pre-login 403 on /api/me and the pre-existing favicon 404."
review-gaps: "Both defects from the first review are fixed and verified; no DoD clause fails. Non-blocking notes carried forward: (4) a step whose parent is hidden by the current filters renders as a plain top-level task in chart and table (no index, project shown); (5) the track is role=group with tabindex 0 and an Enter action, so only its aria-label text hints that it is actionable; (7) pre-existing inline style attributes in other dialog templates (app.js ~319, 333, 338, 401-407, 435, 614, 755) are dropped by the CSP - follow-up, not this ticket; (6) browser evidence still lives only in the uncommitted scratchpad, and I again exercised Owner only, not Viewer. New minor observations, none blocking: the #gantt keydown Escape branch is now redundant with the document listener (harmless duplicate); in the 3px band where the two lane hit areas meet (20-23px of a 44px track) a lane-1 step that is already hovered wins because :hover sets z-index 2 - measured 31px instead of 34px for lane 1 in the overlap column, still well above 20px; after Escape dismisses a hover-only tooltip it does not reappear until the pointer leaves and re-enters the segment, which is the expected dismissal behaviour."
review-verdict: "go: the diff matches the definition of done. The two blocking defects (hover-only tooltip not dismissible with Escape; lane-2 step hit area overpainted by a later lane-1 step) are fixed in ad0549b and confirmed hands-on in Chromium with an independent server-side fixture; 119 tests pass, node --check and git diff --check are clean, authorization surface unchanged. Remaining notes are minor follow-ups. Ready for a person to sign off."
review-check: "1. cd /workspace/project-astra and run .venv/bin/python tests/run.py -> the last lines read \"Ran 119 tests\" and \"OK\".  2. Run node --check src/astra/static/app.js -> prints nothing.  3. Run .venv/bin/python tests/ui_fixture_server.py, open http://127.0.0.1:8766 in Chrome at a window at least 1200px wide, sign in as owner@example.org with password \"correct horse battery\".  4. In the New task form create a task \"Board pack\" in project Browser Acceptance with start 2026-09-01 and due 2026-10-30. Then create \"Collect inputs\" (Parent = Board pack, start 2026-09-01, due 2026-09-10), \"Draft narrative\" (Parent = Board pack, start 2026-09-11, due 2026-09-24) and \"Board minutes\" (Parent = Board pack, no dates).  5. Look at the Portfolio Gantt: the Board pack row shows one light bar with two coloured segments labelled 1 and 2 (blue, orange), a dashed chip \"1 step needs dates\" to its right, a chevron next to the task name, and a legend row under the chart pairing 1..7 with colours.  6. Hover segment 1 and wait half a second: a dark tooltip appears reading \"Step 1 of 3 - Collect inputs\", \"Owner: ...\", \"Sep 1, 2026 -> Sep 10, 2026 - 10 days\", status and due state.  7. FIXED DEFECT 1 CHECK: without clicking anything first, with the mouse still on the segment, press Escape -> the tooltip disappears immediately. Move the mouse off the bar and back onto segment 1 -> it appears again.  8. Click the empty part of the light bar (not a segment) to focus it, press the Right arrow -> segment 1 gets a teal focus ring and the tooltip; press Escape -> the tooltip closes; press Enter -> the Collect inputs dialog opens with a \"Parent: Board pack\" link at the top; press Escape -> the dialog closes and the focus ring is back on segment 1.  9. Click segment 2 with the mouse -> the Draft narrative dialog opens; close it.  10. Click the chevron left of Board pack -> three child rows appear and the chevron points down; reload the page -> the rows are still expanded.  11. Tick the \"Schedule table\" box beside the legend -> the chart is replaced by a table whose header row reads Project, Task, Step #, Step, Owner, Start, Due, Status, Due state, Critical path, with a Board pack row followed by three step rows; untick it to return.  12. Open DevTools device toolbar at 390px wide (with the box unticked and after clearing localStorage key astra.gantt.view, or in a fresh private window) -> the table is shown by default and the page does not scroll sideways.  13. FIXED DEFECT 2 CHECK: create a parent \"Overlap\" (start 2026-09-01, due 2027-05-30) with steps A (start 2026-09-01, due 2026-11-30), B (start 2026-10-15, due 2027-03-31) and C (start 2026-12-01, due 2027-02-28). The Overlap bar is taller and B sits on a second, lower lane under A and C. Hover the top 2-3 pixels of B where C sits directly above it -> the tooltip reads \"Step 2 of 3 - B\" (not C); click there -> the B dialog opens with \"Parent: Overlap\"; press Escape to close. Hover the middle of C -> the tooltip reads \"Step 3 of 3 - C\".  14. Create a fourth step under Board pack with a single day (start and due 2026-10-07) and a far task ending 2028-12-31 so the chart scale shrinks -> a \"+1\" chip appears at the start of the Board pack bar; click it -> a list headed \"1 more step not drawn at this scale (too small or overlapping)\" opens; click on empty page background so nothing is focused, press Escape -> the list closes.  15. Optional: DevTools > Rendering > emulate prefers-reduced-motion: reduce -> hovering a segment no longer lifts it."
---

# Gantt: visible colour-coded task steps with hover and click detail

- [x] Extend service.list_subtasks to return start_date, criticality, progress, parent_task_id, owner_user_id; write the unit test (tests/test_core.py) and HTTP test (tests/test_web.py) first, run red, then make them green
  proof: src/astra/service.py:1248 list_subtasks; tests/test_core.py:1400 test_list_subtasks_returns_step_schedule_shape; tests/test_web.py:680 test_task_detail_subtasks_carry_step_schedule_fields_over_http (both red on KeyError start_date before, green after)
- [x] app.js renderGantt: group state.tasks by parent_task_id; render parent as a tinted state-coloured track with one button.step per dated child positioned via the CSSOM (CSP blocks inline style attributes); index label always, title when wide; <8px steps fold into a +N overflow disclosure; undated children become an 'n steps need dates' chip; chevron expand/collapse to child rows remembered per parent in localStorage
  proof: src/astra/static/app.js:85 groupSteps, :108 applyGeometry (CSSOM, not inline style), :109 renderGantt (track, stepButton, +N overflow, undated chip, derived track, expand rows), :243 toggleSteps (localStorage astra.gantt.expanded)
- [x] Shared #step-tip role=tooltip shown on mouseenter (150ms) and focus, hidden on mouseleave/blur/Escape, hoverable, text-only, aria-describedby while visible; roving tabindex inside each parent bar (Left/Right/Home/End, Enter/Space opens detail through the existing [data-detail] handler)
  proof: src/astra/static/app.js:239 showTip/hideTip + wireGantt mouseover/mouseout/focusin/focusout, :263 roving keydown (ArrowLeft/Right/Home/End, Enter/Space); index.html #step-tip role=tooltip; browser evidence kbd section
- [x] Detail dialog: Parent back-link button when task.parent_title exists; buildSubtasks shows step swatch + index; step dates from the extended list_subtasks shape
  proof: src/astra/static/app.js:544 parentLink in renderDetail, :651 buildSubtasks swatch + index + dates
- [x] style.css: --step-1..--step-7 Okabe-Ito tokens, 22% tint fill + solid bottom stripe + dark text, completed/cancelled desaturated with glyph, on_hold stripes, prefers-reduced-motion, 44px hit area, visible focus ring, legend row with index + colour
  proof: src/astra/static/style.css:16 --step-1..7 + tints, :175 .bar.track, :187 .step::before 44px hit area, .step.done/.hold/.crit/.wrap, :242 prefers-reduced-motion, index.html #step-legend
- [x] Schedule table toggle beside the legend swapping #gantt for a real <table> (Project, Task, Step #, Step, Owner, Start, Due, Status, Due state, Critical path) with caption and scope=col headers, same filtered data, default at max-width 760px
  proof: src/astra/static/app.js:217 renderScheduleTable (caption, scope=col, 10 columns); index.html #view-table + #schedule-table; currentView() defaults to table under (max-width: 760px)
- [x] Verify: node --check app.js, full suite, git diff --check; Playwright evidence for Owner and Viewer at 1440x900 and 390x844 (collapsed, tooltip, expanded, step dialog with Parent link, table, phone, keyboard path, console errors); tick DoD with proofs, note, move to review
  proof: node --check clean; tests/run.py 119 pass (117 before); git diff --check clean; scratchpad/gantt/browser-evidence.md + 20 PNGs (Owner and Viewer, 1440x900 and 390x844), no new console errors

## Definition of Done

- [x] Dated subtasks render as numbered, coloured segments inside the parent task bar on the Gantt (one Okabe-Ito hue per step index, index label always visible, title when the segment is wide enough); hover and keyboard focus show a tooltip with step name, owner, start/due dates, duration, status and due state, dismissible with Escape; clicking or pressing Enter/Space on a step opens that step's detail dialog, which shows a Parent back-link to the parent task; a chevron expands/collapses the child rows under the parent and the choice is remembered per parent in the browser; undated steps are shown as an 'n steps need dates' chip and never given invented dates; colour is never the sole carrier: every segment carries its index and a legend lists index + colour; a Schedule table toggle shows the same data as a real table (Project, Task, Step #, Step, Owner, Start, Due, Status, Due state, Critical path) and is the default at phone width; prefers-reduced-motion disables the transitions; steps narrower than 8px fold into a +N overflow disclosure; service.list_subtasks returns start_date, criticality, progress, parent_task_id and owner_user_id with unit and HTTP tests for the shape; node --check on app.js and the full test suite pass; browser evidence captured for Owner and Viewer at 1440 and 390 widths.
  proof: Segments: app.js:109 renderGantt stepButton (index label always, title >=72px, owner chip >=110px, Okabe-Ito hue = stepHue(idx)); tooltip on hover+focus, Escape: app.js:239 showTip + wireGantt; click/Enter/Space -> openDetail with Parent back-link app.js:544; expand/collapse remembered: app.js:243 toggleSteps; undated chip and derived track: renderGantt; legend: index.html #step-legend; Schedule table: app.js:217 + currentView() phone default; reduced motion: style.css:242; +N overflow: renderGantt overflow list; list_subtasks shape: service.py:1248 with test_core.py:1400 and test_web.py:680; node --check clean; 119 tests pass; browser evidence: scratchpad/gantt/browser-evidence.md (Owner+Viewer, 1440 and 390)

## Options

- [ ] brainstorm
- [x] planning

## Plan

<Steps, in order — filled in by the pre-process step, or by you.>

## Progress
- **2026-09-21 16:10 · Claude** — Implemented the steps feature on branch claude/gantt-steps (stacked on claude/hs3jry-complete 6a82463); code commit 33d237f.
Found: the CSP header (web.py:465) is style-src 'self', so every inline style attribute in the Gantt was being dropped; all bars sat at left 0 in the HS3JRY screenshots. Fixed as part of this work by emitting data-x/data-w and applying positions through the CSSOM (applyGeometry). No CSP console errors remain for the Gantt.
Found: .bar.undated is position:static/inline-block, so a parent with no dates of its own broke the track (steps escaped). Parents with dated steps now get class 'derived' (dashed track) instead of 'undated'.
Decision: step index and hue come from ALL children in state.tasks (stable across filters); only the drawn set follows the filters. Same ordering (start||due, then title) in buildSubtasks so dialog and bar agree.
Decision: plain bars (no steps) also became buttons with the same tooltip so hover works on every bar, not only steps.
Decision: zoom (Week/Month/Quarter) was skipped - optional in the brief and the rest already touches renderGantt heavily; the +N overflow disclosure covers small steps at the current scale. Follow-up ticket if Aly wants it.
Dead end: Playwright wait_for_selector('#detail-dialog:not([open])') never resolves because a closed dialog is not visible; use wait_for_function on .open.
Dead end: pkill -f <script name> also kills the invoking shell when the name is in the command line; check with pgrep first.
Noise: every jaira write prints 'HS3JRY/D73AQW could not be sent: gitref: fatal: expected acknowledgments, received packfile' - a jaira sync/transport error in this container, not a gate refusal; the ticket file on disk updated each time.
Refusal read and satisfied: backlog->brainstorm was refused because the brainstorm option is not ticked; the ticket already had a goal so it moved backlog->todo->pre-process->in-progress instead.
Evidence: scratchpad gantt/browser-evidence.md (not committed); fixture server extended in scratchpad only, stopped, no listener on 8768.
- **2026-09-21 16:12 · Claude** — Code review (medium) on 6a82463..HEAD found two real issues, fixed in a follow-up commit: fmtDay rendered UTC-parsed ISO dates in the local zone (viewers west of UTC saw the previous day in the tooltip) - now formatted with timeZone UTC; new Set(storage.get('astra.gantt.expanded')) would throw at load on a non-array stored value - now guarded with Array.isArray. node --check clean after both.
- **2026-09-21 16:24 · Claude** — Independent review (no-go, sent back from review): (1) DoD clause 'dismissible with Escape' fails for a hover-only tooltip because the Escape handler is bound to #gantt keydown; with focus on body the key never reaches it (verified in headless Chromium). Bind Escape at document level. (2) In two-lane tracks the 44px ::before hit area of a later lane-1 step covers the top ~9px of an overlapping lane-2 step, so hover/click there target the wrong step (verified with elementFromPoint). Shrink the pseudo area under .lanes-2 or raise .lane-2 z-index. Minor: +N label says 'too small to draw' for lane-limit overflow too; steps whose parent is filtered out lose their index; browser evidence is uncommitted scratchpad only. Everything else in the DoD checked out hands-on at 1440 and 390; 119 tests OK; node --check clean; no new route or write path.
- **2026-09-21 16:31 · Claude** — Review send-back (3fccce6) fixed. Wrong: (1) Escape did not dismiss a hover-only tooltip because the handler was on #gantt keydown and focus was still on body; (2) on lanes-2 tracks the 44px .step::before hit area of a later lane-1 step painted over the visible top of an overlapping lane-2 step, so hover/click on its top ~9px hit the lane-1 step. Fix (ad0549b): document-level Escape keydown calls hideTip+closeMoreLists (focused-step path kept); on .lanes-2 tracks each lane's pseudo hit area stays in its own lane (lane 1 -13px/-3px, lane 2 -3px/-13px) and .lane-2 has z-index 1; +N chip/list relabelled 'not drawn at this scale (too small or overlapping)' since overflow also holds lane-limit steps; hideTip already removed aria-describedby (confirmed). Verified: node --check clean; tests/run.py 119 OK; git diff --check clean; Playwright Chromium against the reviewer fixture (scratchpad/gantt/fix-probe.json): hover step 1 with focus on BODY, Escape -> #step-tip hidden and aria-describedby removed; A/B/C overlap (B lane 2 under A and C): elementFromPoint at 1,4,7,10,17px inside B returns B under both A and C, lane-1 steps still hit themselves at 1/9/17px, hover top of B shows 'Step 2 of 3 · B', click there requests /api/tasks/b; +N list open with focus on body closes on Escape. Not fixed here (review minor notes 4,5,7): filtered-out parent renders step as plain task; role=group track; pre-existing inline styles in other dialog templates.
- **2026-09-21 16:39 · Claude** — Independent re-review of ad0549b (go): hover-only Escape and lane-2 hit area both verified in headless Chromium against a server-side Overlap A/B/C fixture (scratchpad/review/rereview.json); measured hit heights lane 1 34px, lane 2 34px, single lane 44px; native dialog Escape and typing in inputs unaffected; 119 tests OK; node --check and git diff --check clean. Carried as non-blocking follow-ups: first-review notes 4 (filtered-out parent loses step index), 5 (role=group track actionable only via label), 6 (evidence uncommitted, Viewer not exercised), 7 (pre-existing inline styles elsewhere); the #gantt Escape branch is now a harmless duplicate of the document listener.
