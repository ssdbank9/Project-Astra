---
id: 01M2JNPRGYD7TGGSHZV16G89SJ
title: Attachments on tasks
status: signoff
ready: true
creator: Aly Jafferani
assignee: Claude
goal: "Attach files to a task (deliverables and supporting docs), stored by Astra and access-controlled, so work products live with the task."
context: "From the owner's template request ('where I can add attachments'). NEW storage capability — Astra stores no files today. DESIGN to settle before build: storage location (proposed %LOCALAPPDATA%/AstraProjectTracker/attachments for the local pilot), size cap, and that these task attachments are DISTINCT from EPO's evidence bytes in the archive (section 4 storage boundary). Foundational for templates-with-attachments (8B9NBH) and the final-results repository."
definition-of-done: "The App Owner links a file inside an ASTRA_ATTACHMENT_ROOTS folder to a task (path, display name, note, added-by, time; no bytes stored); unsafe paths (UNC, device, URL, relative, '..', outside the roots) are refused before any filesystem check; authorized users see the link in task detail; add/remove are App Owner-only and audited; a link marked as a final result cannot be removed until it is explicitly unmarked (409, audited); unit + HTTP tests incl. authorization and CSRF denials; existing tests green. Upload, size cap, sha256 and download moved to MXY7BG."
tags:
  - astra
  - asana
blocked-by: []
related: []
commits: []
created-at: 2026-09-15T13:57:04Z
updated-at: 2026-09-24T17:07:16Z
updated-by: Claude
claimed-by: vm-1221
claimed-at: 2026-09-24T16:18:13Z
question: ""
outcome-what: "Attachment links: one path check (validate_attachment_path in service.py) refuses empty, over-1024-character, control-character, relative, '..', UNC/network, \\\\?\\ and \\\\.\\ device, reserved device names (CON, NUL, COM1-9, LPT1-9), URL and file: paths, and any path outside the folders in ASTRA_ATTACHMENT_ROOTS (unset = links disabled); after the text check, realpath must also stay inside a folder. It runs on add (HTTP 400), on Excel/CSV import (row warning W_ATTACHMENT_PATH_SKIPPED, row still imports) and on template apply (link not copied). The file-exists check runs only on paths that pass; other stored rows report exists=None and are never looked up. Removing an attachment that is a final result is refused with 409 'Unmark the final result first.', recorded as attachment_removal_blocked (reason final_result) and notified to the other owners. 13 new tests (core, import, HTTP authorization matrix, CSRF, 404, 409, 400, viewer render); test path fixtures moved into an allowed folder (tests/link_roots.py). README and excel-import.md updated. No schema change."
outcome-why: "The 2026-09-19 review was NO-GO: any stored path reached os.path.exists on every task read (an existence oracle, and on Windows a UNC path triggers SMB access); deleting an attachment silently cascade-deleted its final result with no audit; and there were no HTTP denial tests. Aly (Slack ts 1790263718.995539) asked to fix only these three on today's link model."
outcome-resolves: "Owner decision Items 1-4 on this ticket: Item 1 allowlisted folders plus UNC/device rejection before any existence check; Item 2 removal blocked while marked, blocked attempt audited and notified, explicit audited unmark first; Item 3 viewers get no form (already true, now tested); Item 4 HTTP authorization and CSRF coverage. Items 5-16 (hosted upload/storage/download) are on MXY7BG. The DoD text still describes uploads and is not literally met; see the DoD MISMATCH note."
review-summary: "Adds one module-level check, `validate_attachment_path` (src/astra/service.py:101-181). It refuses empty, >1024-char, control/bidi, URL/`file:`, UNC/`\\\\?\\`/`\\\\.\\`, relative/drive-relative, colon-after-drive (ADS), `..`/dot-space-only segments and reserved device names. The path must then sit inside a folder listed in `ASTRA_ATTACHMENT_ROOTS` (unset means links are off), and its realpath must also stay inside one. It runs on add (HTTP 400), on Excel import (row warning `W_ATTACHMENT_PATH_SKIPPED`, re-checked at commit) and on template apply (the link is skipped). `_path_exists` validates before `os.path.exists`. A stored row that fails returns `exists=None` and the filesystem is never touched. That closes finding (a). `remove_task_attachment` now checks `final_results` inside `BEGIN IMMEDIATE`. When the link is marked it deletes nothing, writes `attachment_removal_blocked` (reason `final_result`), notifies the other active owners and raises Conflict, which maps to 409. Unmarking first and then removing still works. That closes finding (b). 13 new tests cover the HTTP viewer/chairman/outsider matrix, missing/wrong/foreign CSRF, 404 for mismatched ids, 409, 400 and the viewer render. That closes finding (c). No schema change. Hosted uploads (Items 5-16) are moved to backlog ticket MXY7BG."
review-gaps: "1. MEDIUM, Owner decision, src/astra/service.py:2281-2289 with :3277 (`_notify_owners` skips the actor). Item 2 says \"notify the App Owner on ALL attempts and outcomes\". Only App Owners can remove or unmark, so the owner who acts is never notified. With a single owner, nobody is notified of a blocked removal, an unmark or a removal. Each one is still audited as a task event. The writer disclosed this, and it follows the existing GTEYTG rule. Aly should accept it at signoff or ask for self-notification. 2. LOW, Windows residual, src/astra/service.py:176-180 and :2314. After the text check passes, `realpath` and `os.path.exists` follow reparse points inside an allowed folder, on every task read. Any existence check would do the same. Exposure is limited: the roots are chosen by the admin, NTFS junctions can target only local volumes, and a symlink to UNC needs SeCreateSymbolicLinkPrivilege or Developer Mode. This is not a remaining form of (a). Possible hardening: lstat each component, or tell admins not to allow folders that untrusted users can write to. 3. LOW, src/astra/service.py:147-153 and README.md:186-189. A root of `C:\\` or `/` is accepted and allows the whole drive, which brings back an existence oracle for everything on it (for example `/etc/shadow`). Malformed entries are dropped silently. Suggest refusing drive or filesystem roots, or at least warning in the README. 4. LOW, src/astra/service.py:114,121. Only Cc characters and bidi controls are refused. Zero-width and format characters (U+200B, U+FEFF) and U+2028 pass, which allows look-alike path or display-name spoofing in the UI. Not a filesystem risk. 5. LOW, src/astra/service.py:2631-2636. Template apply drops a failing link silently. The Owner gets no message that a link was not copied (the writer notes there is no warning channel). 6. LOW, src/astra/static/app.js:648. `EVENT_LABELS` has no label for `attachment_removal_blocked`, so history shows the raw kind. The non-owner variant already had this before this change. 7. LOW, ticket hygiene, .jaira/tickets/01M2JNPRGYD7TGGSHZV16G89SJ-attachments-on-tasks.md:22,36-37. The `question:` frontmatter is from 2026-09-15. It asks whether managers may delete, which was superseded on 2026-09-20. DoD 1 describes upload, sha256, size cap and download, and is ticked with a stale proof (\"managers can add/remove\", \"81 pass\"). The work does not literally meet it. The writer disclosed this and the approved scope explains it (see the recommendation under review-verdict). 8. LOW, traceability. jaira's derived commit list for the review lane includes `c25e1f6 chore: import Astra baseline` (a 3.9 MB diff), because the ticket file was born in that commit. The logbook will stamp that commit on the ticket. The real commits are 0a64320 and 359bd15. 9. LOW, MXY7BG tags (.jaira/tickets/01M3A3XCTCPXATBPQGVRMXY7BG-...md, tags). `asana` on this board marks Asana-parity product features (C86ZMY Sections, A48JEX Kanban, RWMRKM My Work, ...). Hosted storage and download security is not one, and the tag was inherited from 6G89SJ. `astra` alone fits. Otherwise the context is good: it leads with what is wrong, names paths, lists Items 5-16 and states the open questions. Assignee is Aly, lane is backlog, and it is related to 6G89SJ. No high-severity gap was found within the approved scope. Tried and found not exploitable: `C:foo`, `C:\\allowed\\..\\x`, `\\\\?\\UNC\\`, `\\\\?\\C:\\`, `\\\\.\\`, `//srv`, `\\\\srv`, `\\/srv`, `\\??\\`, `CON.`, `nul .txt`, `con.txt.bak`, `COM¹`, `CONIN$`, `file.txt:stream`, `::$DATA`, NUL, U+202E, U+0085, >1024 chars, mixed `/` and `\\`, `C:\\Allowed2` against root `C:\\Allowed`, root with or without a trailing separator, case differences, full-width `ｃｏｎ`, U+2215 division slash, `...` and `.. ` segments, `///etc`, relative/UNC/URL/`C:`/`..` roots in the environment variable, empty and whitespace entries, a symlink escape (POSIX), legacy UNC/device/outside rows (never dereferenced), and a race between remove and mark (serialised by BEGIN IMMEDIATE). The codebase has no other delete path that could cascade a final result: no task, project, submission or user deletes, and no DROP or table-rebuild migration."
review-verdict: "Independent review 4 (2026-09-24) of 0a64320..359bd15: approve-with-follow-ups. All three NO-GO findings are fixed on the link model and each fix is pinned by a test that fails when the fix is reverted. What remains is either a documented Owner decision (gap 1) or low-severity hardening and hygiene. Recommendation for Aly at signoff on DoD 1: do not accept the current tick as it stands. Either reword it: `jaira dod 6G89SJ 1 --text \"The App Owner links a file inside an ASTRA_ATTACHMENT_ROOTS folder to a task (path, display name, note, added-by, time; no bytes stored); unsafe paths (UNC, device, URL, relative, '..', outside the roots) are refused before any filesystem check; authorized users see the link in task detail; add/remove are App Owner-only and audited; a link marked as a final result cannot be removed until it is explicitly unmarked (409, audited); unit + HTTP tests incl. authorization and CSRF denials; existing tests green. Upload, size cap, sha256 and download moved to MXY7BG.\"` then re-proof it with `jaira dod 6G89SJ 1 --done --proof \"service.py validate_attachment_path + remove_task_attachment guard; tests test_attachment_* in test_core/test_web/test_import; tests/run.py Ran 409 OK\"`. Or mark it `--superseded` and add a new item worded as above. Also clear the stale `question:` field."
review-check: "How a person checks it (Windows, repo root, branch codex/migration-safety-remediation at 359bd15): 1. Run `.venv\\Scripts\\python.exe tests\\run.py`. The last lines should read `Ran 409 tests` and `OK`. 2. Run `.venv\\Scripts\\python.exe -m unittest -v test_core.AstraCoreTests.test_attachment_path_validation_rejects_every_unsafe_shape` from `tests\\`. It should show OK, with the UNC, device, `..`, URL and outside-root subtests. 3. Start the app without the variable: `Remove-Item Env:ASTRA_ATTACHMENT_ROOTS -ErrorAction SilentlyContinue; astra serve`. Try to link any file on a task. You should see \"Attachment links are disabled until the installation sets allowed folders (ASTRA_ATTACHMENT_ROOTS).\" 4. Stop the app, run `$env:ASTRA_ATTACHMENT_ROOTS = \"D:\\Shared\\Astra\"` and `astra serve`, then link `D:\\Shared\\Astra\\memo.pdf`. The link should be added. 5. Try `\\\\server\\share\\x.pdf`. You should see \"Network (UNC) and device paths cannot be linked.\" Try `D:\\Other\\x.pdf`. You should see \"outside the folders allowed\". 6. Mark the step-4 link as a final result, then click Remove. You should get \"Unmark the final result first.\" and the link and final result should both remain. The history should show attachment_removal_blocked. A second owner, if there is one, should get a notification. 7. Unmark the final result, then Remove. It should succeed, and the history should show final_result_unmarked and then attachment_removed. 8. Log in as a project viewer and open the task. You should see the link and Copy path, but no \"Link file\" form and no Remove button. What this reviewer ran (Linux copy of the repo, read-only on /workspace/project-astra): - `cp -a` into scratch, then `tests/run.py` on the copy: **Ran 409 tests in 201.9s, OK** (396 before, +13). - `git show 359bd15 | grep -c $'\\r'` gave 0 (LF only). `git diff --check` was clean. `node --check app.js` passed. No untracked files. - Commit subject `fix(6G89SJ): ...` with the required trailers. Ticket frontmatter was written by the CLI: status review, assignee Claude, 6/6 plan steps with proof, 3 notes. MXY7BG exists and was committed. `jaira validate` reported errors false (only the old warnings). - Fuzzed `validate_attachment_path` with about 60 ntpath inputs, about 20 posixpath inputs and 14 root variants (all listed under review-gaps). Nothing escaped. - grep over every filesystem call on an attachment path: only `_path_exists` (os.path.exists) and the validator's realpath touch the filesystem, and both are gated. The CSV export, final-results list, import and template code only carry the path text. - Reverted one hunk at a time and ran the matching test. Each revert made the test FAIL, so each test pins its fix: - E1: final-result guard reverted. The core test and the HTTP 409 test failed (2 failures). - E2: validation in `_path_exists` removed. test_stored_path_that_fails_validation_is_never_looked_up failed. - E3: validation on add removed. The add/HTTP 400/symlink tests failed (14 subtest failures). - E4: template-apply validation removed. The template test failed. - E5: importer given an identity check. The import test failed. - E6: realpath symlink check removed. The symlink test failed. - E7: separator-less prefix match (the `Allowed2` bug). The shape test failed. - E8: owner check on add dropped. The HTTP matrix test failed (201 != 403). - E9: CSRF check on DELETE removed. The CSRF test failed. - E10: blocked-removal event dropped. The core test failed (0 != 1). - Not done: a Windows run (junction/realpath behaviour is reasoned, not executed) and a browser check."
---

# Attachments on tasks

## Definition of Done

- [x] The App Owner links a file inside an ASTRA_ATTACHMENT_ROOTS folder to a task (path, display name, note, added-by, time; no bytes stored); unsafe paths (UNC, device, URL, relative, '..', outside the roots) are refused before any filesystem check; authorized users see the link in task detail; add/remove are App Owner-only and audited; a link marked as a final result cannot be removed until it is explicitly unmarked (409, audited); unit + HTTP tests incl. authorization and CSRF denials; existing tests green. Upload, size cap, sha256 and download moved to MXY7BG.
  proof: 359bd15: service.py validate_attachment_path (add, import, template apply, _path_exists) + remove_task_attachment final-result guard (Conflict 409, attachment_removal_blocked). Tests: test_core test_attachment_path_validation_rejects_every_unsafe_shape, test_add_attachment_rejects_unsafe_paths_and_writes_nothing, test_attachment_symlink_out_of_an_allowed_folder_is_rejected, test_stored_path_that_fails_validation_is_never_looked_up, test_attachment_marked_as_final_result_cannot_be_removed, test_template_apply_skips_attachment_links_that_fail_validation, test_attachment_authorization, test_task_attachment_link_lifecycle_and_audit; test_import test_owner_import_skips_attachment_paths_that_fail_validation; test_web test_attachment_authorization_matrix_over_http, test_attachment_mutations_reject_missing_and_wrong_csrf, test_attachment_removal_needs_the_matching_task, test_attachment_marked_as_final_result_is_not_removed_over_http, test_attachment_path_outside_the_rules_is_rejected_over_http, test_attachment_form_is_absent_for_a_viewer_and_unchecked_links_are_not_called_missing, test_task_attachment_link_over_http. tests/run.py Ran 409 tests, OK.

## Options

- [ ] brainstorm
- [ ] planning

## Plan

<Steps, in order — filled in by the pre-process step, or by you.>

- [x] Add _validate_attachment_path in service.py with an ASTRA_ATTACHMENT_ROOTS allowlist; call it in add, Excel/CSV import and template apply
  proof: src/astra/service.py validate_attachment_path (module level, after row_dict); add_task_attachment, _instantiate_tasks, importer.py ImportEngine check_attachment_path + W_ATTACHMENT_PATH_SKIPPED
- [x] Only check existence for validated paths; legacy invalid rows report exists=None and the UI does not say file not found
  proof: service.py AstraService._path_exists returns None without touching the filesystem; test_core test_stored_path_that_fails_validation_is_never_looked_up; app.js buildAttachments unchanged (flags only exists===false)
- [x] Block remove_task_attachment while a final result references it: audit attachment_removal_blocked, notify other owners, 409
  proof: service.py remove_task_attachment final_results guard -> attachment_removal_blocked + Conflict; test_core test_attachment_marked_as_final_result_cannot_be_removed
- [x] Add core, import and HTTP tests: path shapes, blocked removal, import/template skips, authorization matrix, CSRF, 404, 409, 400, viewer render
  proof: tests/test_core.py, tests/test_import.py, tests/test_web.py new 6G89SJ tests; tests/link_roots.py
- [x] Run the full suite, git diff --check, jaira validate; adversarial self-review of the diff
  proof: tests/run.py: Ran 409 tests, OK (was 396); git diff --check clean; jaira validate: no errors (28 undeclared_dependency warnings); node --check app.js ok
- [x] File hosted-uploads backlog ticket for Owner decision Items 5-16; move 6G89SJ to review; commit locally
  proof: MXY7BG filed (backlog, Aly Jafferani); 6G89SJ moved to review; local commit fix(6G89SJ) (not pushed)

## Progress
- **2026-09-15 14:32 · Aly Jafferani** — REQUIREMENT (owner, 2026-09-15): DO NOT build a managed file store. Keep file storage separate — Astra stores only a LINK/reference to a file that lives in a folder outside Astra, not the bytes. Implications for this ticket's design: (1) no copying of file bytes, no size cap, no sha256-of-stored-bytes needed; (2) an 'attachment' record = path/link + metadata (display name, added-by, time, optional note); (3) 'deletion' removes the LINK, never touches the file on disk; (4) opening an attachment opens/reveals the linked file from its folder. TRADEOFF to accept: Astra can gate who sees the LINK in-app (task authorization still applies to the record), but it CANNOT gate the file itself on the filesystem — anyone with folder access can open it. This weakens the original DoD guarantee ('download requires the SAME authorization as the task'); with links, that guarantee covers the link record only. Templates (8B9NBH) then carry links, not copied files; final-results repo (CS93C6) indexes links.
- **2026-09-15 14:44 · Aly Jafferani** — PLAN (link-only attachments): (1) db.py migration v8: new task_attachments table = id, task_id FK ON DELETE CASCADE, path (the local filesystem link; no bytes), display_name, note, added_by FK, added_at; index on (task_id, added_at). No size/sha256 columns per owner link-only decision. (2) service.py: add_task_attachment (gate can_manage_project, path required, display_name defaults to basename), list_task_attachments (gate via get_task = can_view_project; computes best-effort 'exists' flag so dead links show), remove_task_attachment (gate can_manage_project, audited); both mutations go through _event -> owner notified (consistent w/ Z24KVH). New audit kinds attachment_added / attachment_removed. Wire attachments into task_detail(). (3) web.py: POST /api/tasks/{id}/attachments (add), DELETE /api/task-attachments body{task_id,attachment_id} (remove) — mirrors task-reviewers idiom; view rides task_detail. (4) app.js: Attachments section in renderDetail (name, path in monospace + Copy path button, added-by/when, note, Remove for managers); add EVENT_LABELS entries. (5) tests: core (add/list/remove+audit, authorization denial for non-member), web (HTTP add+remove roundtrip). CONSTRAINT flagged: a browser served over http CANNOT open a file:// local path (silently blocked), so UX is Copy-path -> paste into Explorer; a server-side 'reveal in Explorer' (os.startfile) is possible for the local pilot but deliberately deferred (OS-specific, foot-gun, hard to test).
- **2026-09-15 15:15 · Aly Jafferani** — No real-browser visual pass: yolo-chrome MCP failed to connect this session (CONNECTION_CLOSED). Verified instead: 81 tests green incl. HTTP add+delete round-trip; app.js passes node --check; migration v8 boots (web tests exercise it). Browser constraint carried into UX: an http-served page cannot open a file:// local path, so the panel shows the path + a Copy-path button (paste into Explorer). A server-side 'reveal in Explorer' (os.startfile) is feasible for this local pilot but deferred. Follow-ons unblocked: 8B9NBH (templates carry links) and CS93C6 (final-results repo indexes links) can now build on task_attachments.
- **2026-09-15 18:31 · Aly Jafferani** — OWNER DECISION (2026-09-15): restrict attachment DELETION to the App Owner ONLY (not project managers). Keep copy-path opening as-is. Tighten delete authorization.
- **2026-09-19 06:14 · Aly Jafferani** — HANDOFF 2026-09-19 (Claude->Codex): see astra_project_tracker/CODEX_HANDOFF_2026-09-19.md. DO THIS ONE FIRST - it unblocks 8B9NBH and CS93C6. Latest change: attachment DELETION gated to App Owner only (require_owner in remove_task_attachment; Remove hidden from non-owners); adding still allowed for project managers. In REVIEW awaiting the model review pass. Re-claim first (claim is stale ~90h). Baseline: python tests/run.py = 102 green. After review pass: jaira move --to signoff with review-summary/gaps/verdict/check, and commit the ticket file with the code in one commit whose message names the handle (board is unshared, so the handle is what makes the commit list derivable).
- **2026-09-19 08:22 · Aly Jafferani** — REVIEW NO-GO 2026-09-19: current-state review found (1) arbitrary manager-supplied paths are dereferenced by os.path.exists, creating an existence oracle and possible UNC/SMB access; (2) attachment deletion cascades a linked final_results row without a final_result_unmarked audit event; (3) HTTP authorization denial coverage is missing and the add form is visible to viewers. The implementation otherwise matches the effective link-only/owner-delete decision and 102 project-venv tests pass. Source-diff traceability is weak because feature code exists only in snapshot 190fde5 while Jaira supplies board commit a7afaa1.
- **2026-09-19 08:36 · Aly Jafferani** — OWNER DECISION 2026-09-19 — Item 1: use an App Owner-approved allowlist of local root folders. An attachment path must resolve inside an approved root or Astra must block it. Reject UNC/network paths and Windows device paths. Existence checking is permitted only after this validation. Configuration details may be finalized during rework, but unapproved paths must not be linkable.
- **2026-09-19 08:44 · Aly Jafferani** — OWNER DECISION 2026-09-19 — Item 2: if an attachment is marked as a final result, block removal of its attachment link until the final-result designation is explicitly removed. The explicit unmark must be audited. Notify the App Owner on ALL relevant attempts and outcomes: every blocked attempt to remove an attachment that is still a final result, every explicit final-result unmark, and every successful attachment-link removal. Notifications must identify the actor, task, attachment, action/outcome, and time. The underlying file must never be deleted.
- **2026-09-19 08:54 · Aly Jafferani** — OWNER DECISION 2026-09-19 — Item 3: hide the Link file form completely from users who cannot manage the project. Authorized viewers may still see existing attachment metadata and use Copy path. Project managers and the App Owner may see and use the add form. Keep service/API authorization enforcement even when the UI control is hidden.
- **2026-09-19 09:01 · Aly Jafferani** — OWNER DECISION 2026-09-19 — Item 4: add complete HTTP authorization and CSRF coverage for attachments. Prove: App Owner can add/remove; project manager can add but cannot remove; viewer can view but cannot add/remove; outsider cannot view the task or attachment records; missing/invalid CSRF cannot add/remove. New deployment fact: Astra will be HOSTED. Reassess filesystem-path assumptions before rework because client-local paths are not available to a hosted server.
- **2026-09-19 09:20 · Aly Jafferani** — OWNER DECISION 2026-09-19 — Item 5: build BOTH hosted attachment types. Type A: approved HTTPS document-repository link, restricted to an App Owner-managed allowlist of domains/services; Astra stores URL + metadata and opens it in the browser. Type B: managed file upload; Astra stores the bytes and serves downloads only after applying the same authorization as the task. This SUPERSEDES the earlier local-root/UNC path-link model for the hosted product: do not accept client-local, server-local, device, or UNC filesystem paths as attachments. Each attachment must be clearly typed and labelled as external link or Astra-managed upload.
- **2026-09-19 10:22 · Aly Jafferani** — OWNER DECISION 2026-09-19 — Item 6: support TWO selectable managed-upload storage backends behind one storage interface. Backend A: Oracle Cloud Infrastructure storage for the hosted Oracle deployment (prefer private OCI Object Storage rather than a publicly exposed bucket). Backend B: protected private-server filesystem storage for a private/on-premises deployment. Both backends must share identical task authorization, private download handling, metadata, SHA-256 integrity, audit, notification, deletion, backup/recovery documentation, and tests. External approved HTTPS links remain a separate attachment type.
- **2026-09-19 10:24 · Aly Jafferani** — OWNER DECISION 2026-09-19 — Item 7: one active managed-upload storage backend per Astra installation. The App Owner selects either private OCI Object Storage or protected private-server filesystem storage during configuration. Do not let users or projects mix destinations. A later backend change must use a controlled migration that copies, verifies SHA-256 and size, updates metadata only after verification, and supports rollback; never silently strand existing files.
- **2026-09-19 10:31 · Aly Jafferani** — OWNER DECISION 2026-09-19 — Item 8: managed uploads default to a 25 MB maximum. The App Owner may configure the installation limit up to an absolute hard ceiling of 250 MB; no role may exceed that ceiling. Stream uploads rather than buffering the whole file in memory, enforce the limit during streaming, remove partial objects/files on rejection or failure, and direct larger files to an approved external HTTPS repository link.
- **2026-09-19 10:35 · Aly Jafferani** — OWNER DECISION 2026-09-19 — Item 9: use a known-dangerous file-type denylist rather than a narrow business-document allowlist. Reject executable, installer, script, command, library, disk-image, and other configured dangerous types using filename extension, declared MIME type, and detected file signature where practical. Treat mismatches or unidentifiable suspicious content as rejected/quarantined, never as trusted. Make the denylist App Owner-configurable with secure defaults. This policy does not replace malware scanning.
- **2026-09-19 10:37 · Aly Jafferani** — OWNER DECISION / ACCEPTED RISK 2026-09-19 — Item 10: do NOT integrate malware scanning for managed uploads at this stage. Rely on the dangerous-type denylist, extension/MIME/signature consistency checks, and users' endpoint protection. Astra must never label a file safe, clean, or scanned. Record this as an explicit hosted-production risk: permitted document formats can still contain malware, and neither OCI nor private-server storage changes that fact. Keep the storage/scanning interface extensible so a scanner can be added later without rewriting attachment records.
- **2026-09-19 10:40 · Aly Jafferani** — OWNER DECISION / ACCEPTED RISK 2026-09-19 — Item 11: show NO malware warning in the upload or download user interface. Keep the lack of malware scanning disclosed in administrator/security documentation and deployment checks, but do not add user-facing warning text or confirmation friction. Do not make any positive safety/scanning claim in the UI.
- **2026-09-19 10:45 · Aly Jafferani** — OWNER DECISION / ACCEPTED RISK 2026-09-19 — Item 12: permit any syntactically valid HTTPS external attachment URL; do not require an App Owner domain allowlist. Continue to reject non-HTTPS schemes, embedded URL credentials, malformed URLs, and local file/device schemes. Record the accepted risk that links may lead to phishing, tracking, malware-hosting, or unrelated external sites and that destination access is controlled outside Astra.
- **2026-09-19 10:47 · Aly Jafferani** — OWNER DECISION 2026-09-19 — Item 13: Astra must never server-fetch, probe, preview, follow redirects for, or check availability of external attachment URLs. Validate syntax only, store/display the URL, and open it in the user's browser in a new tab using rel=noopener noreferrer. Broken-link status is not provided for external URLs. This avoids SSRF and private-network probing while any HTTPS destination is permitted.
- **2026-09-19 10:48 · Aly Jafferani** — OWNER DECISION 2026-09-19 — Item 14: every managed-file download must be authorized and streamed through Astra for both OCI Object Storage and private-filesystem backends. Do not expose permanent or temporary storage URLs. Recheck task access immediately before transfer; stream rather than buffer; support HTTP download semantics appropriate for files up to 250 MB; audit successful and denied attempts. This must work when Astra is hosted on the owner's desktop: remote users access files through the Astra web application, never through direct desktop filesystem access. Desktop reachability, TLS, firewall and uptime remain deployment concerns.
- **2026-09-19 10:52 · Aly Jafferani** — OWNER DECISION / ACCEPTED RISK 2026-09-19 — Items 14 and 15 update: build BOTH installation-level managed-download modes so the App Owner can decide later. Mode 1: Astra-authorized streaming (task access rechecked for every request; audited). Mode 2: permanent direct storage/static URL (lowest Astra application-processing/bandwidth load, especially for OCI). Permanent URLs explicitly BYPASS ongoing Astra task authorization: anyone with the URL can access/share it until the object/URL is revoked, and later task-access revocation does not revoke prior copies. Never represent direct mode as task-access-controlled. For private desktop storage, direct serving still consumes the desktop's network bandwidth even if it bypasses Astra application streaming. Also support Item 15 Option 2: public Internet deployment through a domain and HTTPS reverse proxy, with hardened authentication, firewall, rate limiting, patching, monitoring and reliable uptime required before production exposure. The active download mode remains an App Owner deployment decision.
- **2026-09-19 10:55 · Aly Jafferani** — OWNER DECISION 2026-09-19 — Item 16: combine secure streaming and permanent direct access per attachment. Every managed upload starts with task-authorized Astra streaming and has NO permanent URL by default. Only the App Owner may explicitly generate a permanent direct link for a specific attachment after a clear confirmation that it bypasses task authorization. Generation and revocation must be audited and notify the App Owner. Task-access revocation does not revoke a permanent link; revocation is separate. OCI uses a direct object-access mechanism; private-server storage uses a separately served public token URL and still consumes server/desktop network bandwidth. Never auto-generate permanent links.
- **2026-09-20 03:13 · Aly Jafferani** — Owner confirmation recorded 2026-09-20: every file/final-result mutation, including attachment deletion, is App Owner-only. Hosted Astra uses authorized managed download/open behavior and never exposes or reveals local Explorer paths.
- **2026-09-24 15:30 · Aly Jafferani** — Sent back from signoff 2026-09-24 to fix the three NO-GO review findings
- **2026-09-24 16:28 · Claude** — Reassigned to Claude with Aly's approval 2026-09-24 (Slack thread ts 1790256175.671249, 'okay proceed' ts 1790263718.995539). Scope narrowed by Aly: fix ONLY the three NO-GO review findings on today's local-path link model (Owner Items 1-4: allowlisted roots + path validation, block removing a final-result attachment with audit, HTTP authorization/CSRF denial tests). Owner decision Items 5-16 (hosted HTTPS links, managed upload, OCI/private-folder storage, 25/250 MB caps, denylist, streamed downloads, permanent links) moved to new backlog ticket MXY7BG, assigned to Aly.
- **2026-09-24 16:28 · Claude** — Design choices (Claude, 2026-09-24): allowlist = env var ASTRA_ATTACHMENT_ROOTS (';'-separated absolute folders; the app reads ASTRA_HOME/ASTRA_SECURE_COOKIES the same way; there is no installation config object). Unset/empty = every path refused. Validation is text-only (ntpath/posixpath) before any filesystem call; after it passes, realpath must also stay inside a root (blocks symlink escapes). Stored rows failing validation report exists=None and are never looked up; app.js already prints '(file not found)' only for exists===false, so no UI change. Blocked removal notifies OTHER active owners only (existing _notify_owners rule: the actor is not self-notified). The inline global_role checks were kept, not switched to require_owner: they write an audit event before raising and use a different message, so require_owner is not a drop-in. Template apply skips a failing link silently, like a dependency whose end is missing (_instantiate_tasks has no warning channel). Import skips it with row warning W_ATTACHMENT_PATH_SKIPPED.
- **2026-09-24 16:31 · Claude** — DoD MISMATCH (Claude, 2026-09-24): the Definition of Done text still describes a managed upload (sha256, size cap, download). This fix stays on the link model Aly chose on 2026-09-15, so that text is NOT literally met and I did not re-tick or re-proof it. The existing tick and its 2026-09-15 proof predate HS3JRY (it says managers can add/remove and '81 pass'), so the proof is stale too. Upload, size caps, sha256 and download are Owner Items 5-16, now on MXY7BG. Aly to decide at signoff: reword DoD 1 to the link model (jaira dod 6G89SJ 1 --text ...) or mark it superseded. Residual risks: (1) on Windows, os.path.realpath opens the path, so an allowed folder that itself holds a junction to a network share would still be followed once before being rejected; the allowed folders are chosen by the installation. (2) The final-results CSV export and Copy path still show local paths (hosted concern, on MXY7BG). (3) The Owner who acts is not notified of their own blocked removal; other active owners are (GTEYTG rule).
- **2026-09-24 16:41 · Claude** — Independent review 4 (2026-09-24, diff 0a64320..359bd15): approve-with-follow-ups. Follow-ups for Aly at signoff: (1) MEDIUM, Owner decision: the acting owner is never self-notified, so a single-owner install gets no notice of blocked removal, unmark or removal (audited as events only); accept or ask for self-notification. (2) LOW hardening: Windows reparse points inside an allowed folder; drive or filesystem roots (C:\ or /) accepted as allowed folders; zero-width/format characters not refused; template apply drops a failing link silently; no EVENT_LABELS entry for attachment_removal_blocked. (3) Hygiene: reword or supersede DoD 1 and clear the stale question field (review-verdict has suggested text); derived commit list includes baseline import c25e1f6; real commits are 0a64320 and 359bd15.
- **2026-09-24 17:07 · Claude** — Signoff prep 2026-09-24 with Aly's approval (Slack ts 1790268694.030539, 'yes update as per your recommendation, and move on'): DoD 1 reworded to the link model and re-proofed with the 359bd15 tests (review 4 recommendation), frontmatter definition-of-done set to the same text, stale question cleared. The acting-owner notice (review 4 gap 1) and the per-person cap on blocked-attempt notices moved to KBWY86.
